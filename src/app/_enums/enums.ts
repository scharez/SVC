
/* Enums */

enum Electiontype {
  SCHULSPRECHER,
  ABTEILUNGSSPRECHER,
  STICHWAHL_SCHULSPRECHER,
  STICHWAHL_ABTEILUNGSSPRECHER
}

enum Electionstate {
  NEW,
  RUNNING,
  STOPPED,
  ENDED
}

enum Department {
  INFORMATIK,
  MEDIENTECHNIK,
  ELEKTRONIK,
  MEDIZINTECHNIK
}
